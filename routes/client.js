const express = require("express");

// Import roles controller
 const {  } = require("../controllers/client"); 

// Create roles router
const clinetRouter = express.Router();


clinetRouter.post("/", );

module.exports = clinetRouter;